export default function ({ title }) {

    return (
        <th className={`px-3 py-3.5 font-normal text-sm hover:bg-gray-700 bg-gray-600 border border-gray-500 text-gray-100 duration-150`}>
            {title}
        </th>
    )
}
